// Escribe tu código aquí:

// Uso de funciones flechas 
// Forma 1 si lleva {} es necesario poner un return de lo contrario no retorna nada
const sumaflecha = (a, b) => {return a + b}
console.log(sumaflecha(2, 5), typeof(sumaflecha))

// Forma 2 sin {} retorna la operacion
const sumaflecha2 = (a, b) => a + b
console.log(sumaflecha2(2 , 6), typeof(sumaflecha2))